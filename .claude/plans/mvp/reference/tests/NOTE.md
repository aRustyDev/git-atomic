# Archived Reference Material

The files in this directory are archived reference material from a prior Helm chart workflow design. They are **not related** to the git-atomic MVP plan.

Retained for historical context only. Do not use as implementation guidance for git-atomic.
